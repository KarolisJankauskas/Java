const krastineA = prompt("Ivesk trikampio a krastine")
const krastineB = prompt("Ivesk trikampio b krastine")
const krastineC = prompt("ivesk trikampio c krastine")

const plotas = (krastineA * krastineB * krastineC) / 2;

console.log(`Trikampio plotas yra ${plotas}`);